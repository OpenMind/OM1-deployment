version: '3.8'

services:
  watchdog:
    build:
      context: .
    image: 453064692066.dkr.ecr.us-east-1.amazonaws.com/openmind/om1_ros2_sdk:latest
    container_name: watchdog
    network_mode: host
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - SENSOR_TIMEOUT_DURATION=30.0
    command: "python3 watchdog/watch_sensor.py"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
